# One run of the commit agent. Started by heron-commit.timer.
[Unit]
Description=heron commit agent
After=network.target

[Service]
Type=oneshot
WorkingDirectory=@REPO@
ExecStart=@REPO@/bin/heron commit
